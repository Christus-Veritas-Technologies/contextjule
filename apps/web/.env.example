# Origin of the API. Everything else the site needs comes from it —
# prices, the launch phase, the current release.
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Google Analytics measurement id, G-XXXXXXXXXX. Leave blank outside
# production: unset means no tag is rendered, so development and preview
# traffic never reaches the real property.
NEXT_PUBLIC_GA_ID=
